
import { City, User, Installer, Job, JobStatus, PaymentStatus, ServiceDefinition } from './types';

export const DB_CITIES: City[] = [
];

export const DB_USERS: User[] = [
  
];

export const INITIAL_SERVICES: ServiceDefinition[] = [

];

export const INITIAL_INSTALLERS: Installer[] = [

];

export const INITIAL_JOBS: Job[] = [

];
